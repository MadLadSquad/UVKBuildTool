#pragma once
#include "Core.hpp"

namespace UBT
{
    void generateMain(const char* startupLevelName, const char* gameName);
    void generateGame();
    void generateDef();
    void generateWrapperAndMod();
}