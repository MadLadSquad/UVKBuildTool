#pragma once
#include "Engine/Engine.hpp"

#ifdef _WIN32
    #ifdef UVK_LIB_COMPILE
        #define ${{ uppercase_name }}_PUBLIC_API __declspec(dllexport)
    #else
        #define ${{ uppercase_name }}_PUBLIC_API __declspec(dllimport)
    #endif
#else
    #define ${{ uppercase_name }}_PUBLIC_API
#endif